════════════════════════════════════════════════════════════════
  GIẢI PHÁP CUỐI CÙNG - CÀI MULTER BỎ QUA LỖI CANVAS
════════════════════════════════════════════════════════════════

❌ VẤN ĐỀ:
   Canvas đang build fail và ngăn multer được cài.

✅ GIẢI PHÁP - Chạy 1 trong các lệnh sau:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CÁCH 1 (Khuyên dùng - Bỏ qua build scripts):
─────────────────────────────────────────────
npm install multer --ignore-scripts --legacy-peer-deps

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CÁCH 2 (Bỏ qua optional dependencies):
───────────────────────────────────────
npm install multer --no-optional --legacy-peer-deps

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CÁCH 3 (Force install):
───────────────────────
npm install multer --force --legacy-peer-deps

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 SAU KHI CÀI:

Kiểm tra multer:
  npm list multer

Nếu thấy multer@1.4.5-lts.x → THÀNH CÔNG! ✅

Chạy server:
  nodemon app.js

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 LƯU Ý:
   - Canvas chỉ cần cho PDF OCR
   - OCR với ảnh vẫn hoạt động bình thường không cần canvas
   - Bạn có thể cài canvas sau nếu cần

════════════════════════════════════════════════════════════════

